import React from 'react'

const FoldersSideBar = () => {
  return (
    <div>
      <h1>This side bar wraps the folder section where the created folders are shown</h1>
    </div>
  )
}

export default FoldersSideBar
