import React from 'react'

const FileType = () => {
  return (
    <div>
      <h1>This allows user to filter the files on 
        the basis of their types, only pdf, word,
        and text files are shown.
      </h1>
    </div>
  )
}

export default FileType
