import React from 'react'

const BackButton = () => {
  return (
    <div>
      <h1>This allows user to navigate back to original directory
        from the current folder.
      </h1>
    </div>
  )
}

export default BackButton
