import React from 'react'

const AllFilesSideBar = () => {
  return (
    <div>
      <h1>This side bar wraps the section where it contains all
        the files that are stored in the system.
      </h1>
    </div>
  )
}

export default AllFilesSideBar
