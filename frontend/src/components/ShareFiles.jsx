import React from "react";

const ShareFiles = () => {
  return (
    <div>
      <h1>
        Allow user to share encrypted or non-encrypted files to other users.
      </h1>
    </div>
  );
};

export default ShareFiles;
